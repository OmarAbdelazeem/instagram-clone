import 'package:flutter/material.dart';

class ExploreVideosScreen extends StatelessWidget {
  const ExploreVideosScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
